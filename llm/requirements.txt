# ── RCA LLM Backend — Python Dependencies ──────────────────────────────────────
# Direct dependencies only — transitive packages (httpx, anyio, starlette, etc.)
# are resolved automatically by pip to avoid version conflicts.

# API framework
fastapi==0.111.0
uvicorn>=0.29.0

# Data models & env config
pydantic
pydantic-settings
python-dotenv

# LLM providers
openai>=1.57.0
google-genai==1.62.0

# Pin httpx to a range all three providers agree on
# - google-genai needs >= 0.28.1
# - openai >= 1.57 works correctly with httpx 0.28.x (removed internal proxies= usage)
httpx>=0.28.1,<1.0

# Vector database (RAG)
weaviate-client

# WebSockets / SSE support
websockets

# Knowledge graph — historical incident similarity search
neo4j>=5.14

# PDF text extraction for chat attachments
pypdf