# ── ProdAI Web App — Python Dependencies ────────────────────────────────────

# API framework
fastapi==0.103.1
hypercorn==0.16.0

# Templates & forms
jinja2==2.11.3
python-multipart

# Auth
# passlib 1.7.4 is incompatible with bcrypt 4.x — pin to 3.x
passlib
# bcrypt removed - using sha256_crypt
pyjwt

# Database
sqlalchemy

# Date parsing
python-dateutil
MarkupSafe==2.0.1
